# TODO

- [ ] Inspect existing auth integration points (AuthContext/useAuth) and confirm how login should be triggered.
- [ ] Create premium Tailwind dark login UI component at `src/components/Auth/Login.jsx`.
- [ ] Wire login form to `useAuth` hook (loginUser, loading/error state).
- [ ] Implement email/username + password fields with validation and inline error feedback.
- [ ] Add loading skeleton overlay + button state during async login.
- [ ] Ensure responsive layout scales cleanly for mobile/tablet/widescreen without truncation.
- [ ] (Optional) Add route/entry integration if app currently renders auth gate (not specified).
