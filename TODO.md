# TODO

- [x] Implement `src/context/AuthContext.jsx` with:
  - [x] `AuthContext`
  - [x] `AuthProvider` wrapper
  - [x] `useAuth` hook
  - [x] Track `user`, `loading`, `error`
  - [x] Provide `loginUser` and `logoutUser`
  - [x] Persist session to `localStorage` and restore on refresh via `useEffect`
  - [x] Ensure exports match requirements
